ARCHIVED STORIES — old & alternate versions
============================================
Kept for reference, not loaded automatically. Each is still a complete, swappable
window.IDN_STORY (same shape as the active story.js) — to play one, copy it over the
top-level story.js and reload, e.g.:

  cp "stories/archive/clod-bathos.complex.story.js" "story.js"

These are also preserved in git history; this folder just keeps them visible.

CONTENTS
  clod-bathos.complex.story.js
        The ORIGINAL, complex "CLOD BATHOS" before the radical simplification —
        persuasion meters, an `approach` tone signal, patience, a forward-ladder
        topology (parley -> entreaty -> supplication), a promise-to-return shortcut,
        and the loneliness arc. SUPERSEDED by the minimal 3-petition binary now in
        stories/clod-bathos.story.js (and active in story.js). Also recoverable via
        `git checkout b66998d -- story.js engine/engine.js`.

  signal.story.js
        "SIGNAL" — a lonely presence hiding in a dead carrier channel; coax it out
        (or drive it off). The original shipped story; exercises every engine feature
        (world params, signals, rules, bindings). Kept here as the alternate/earlier
        story, stored for later rather than deleted.
</content>
